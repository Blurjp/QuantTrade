# Nixpacks configuration for Railway
# Install system dependencies for geospatial packages

[phases.setup]
nixPkgs = ["...", "gdal", "expat", "libxml2"]
nixLibs = ["expat", "libxml2"]
aptPkgs = ["...", "gdal-bin", "libgdal-dev", "python3-gdal", "libexpat1", "libxml2"]
